// ═══════════════════════════════════════════════════
// MODULE 21 — Architecture multi-tenant & white-label
// 5 leçons — Format JSON blocs
// ═══════════════════════════════════════════════════

function blocks(b: object[]): string {
  return JSON.stringify(b);
}

export const MODULE_21_MULTITENANT_LESSONS = [
  // ─── LEÇON 1 : Du sur-mesure au scalable ───
  {
    order: 1,
    module: 21,
    title: "Du sur-mesure au scalable : le mindset multi-tenant",
    slug: "sur-mesure-scalable-mindset-multitenant",
    duration: "20 min",
    description: "Pourquoi construire un produit vendu une seule fois est un suicide financier. L'approche build once, sell many, et comment passer d'une logique projet à une logique produit SaaS.",
    content: blocks([
      { id: "1-1", type: "heading", level: 2, text: "Le piège mortel du sur-mesure" },
      { id: "1-2", type: "text", html: "<p>Tu as construit un chatbot pour un client. Il t'a payé 3000€. Félicitations. Maintenant, pour gagner encore 3000€, tu dois… reconstruire un chatbot pour un autre client. Et ainsi de suite, à l'infini.</p><p>C'est le <strong>piège du sur-mesure</strong> : chaque euro gagné te coûte le même effort. Tu échanges du temps contre de l'argent, exactement comme un salarié — sauf que tu n'as ni congés payés, ni mutuelle.</p><p>Le modèle qui fonctionne ? <strong>Build once, sell many.</strong> Tu construis un produit une fois, et tu le vends à 10, 50, 500 clients. Ton effort initial est amorti sur chaque vente. C'est comme ça que les vrais business SaaS fonctionnent.</p>" },
      { id: "1-3", type: "callout", variant: "warning", html: "<strong>Réalité brutale :</strong> Si ton revenu est directement proportionnel à tes heures de travail, tu n'as pas un business — tu as un job. Le multi-tenant, c'est la sortie de ce piège." },
      { id: "1-4", type: "separator", style: "dots" },

      { id: "1-5", type: "heading", level: 2, text: "Build once, sell many : le modèle SaaS" },
      { id: "1-6", type: "comparison", headers: ["Critère", "Modèle sur-mesure", "Modèle SaaS multi-tenant"], rows: [
        { cells: ["Revenu par projet", "3 000–10 000€ one-shot", "200–500€/mois récurrent × N clients"] },
        { cells: ["Effort par client", "100% à chaque fois", "~5% (onboarding + config)"] },
        { cells: ["Scalabilité", "Linéaire (1 client = 1 projet)", "Exponentielle (1 produit = N clients)"] },
        { cells: ["Valorisation business", "1× CA annuel", "5–10× ARR (Annual Recurring Revenue)"] },
        { cells: ["Vacances", "Le business s'arrête", "Les abonnements tombent quand même"] },
      ]},
      { id: "1-7", type: "text", html: "<p>Regarde les maths : 1 client sur-mesure à 5000€ vs 50 clients SaaS à 200€/mois. Le sur-mesure te donne 5000€ une fois. Le SaaS te donne <strong>10 000€/mois, chaque mois</strong>. Et si tu veux vendre ton business un jour, un SaaS avec 120K€ d'ARR se vend entre 600K€ et 1.2M€. Le sur-mesure ? Quasiment rien.</p>" },
      { id: "1-8", type: "separator", style: "line" },

      { id: "1-9", type: "heading", level: 2, text: "Exemples concrets de la transition" },
      { id: "1-10", type: "steps", steps: [
        { title: "L'agence chatbot → plateforme chatbot", description: "Au lieu de construire un chatbot custom pour chaque client, tu crées une plateforme où chaque client configure son propre chatbot. Tu passes de 3 clients/mois à 50+ clients actifs." },
        { title: "Le freelance automatisation → outil no-code IA", description: "Au lieu d'automatiser les process un par un, tu crées un outil que les clients utilisent eux-mêmes. Tu factures un abonnement mensuel au lieu d'un one-shot." },
        { title: "Le développeur d'agents → marketplace d'agents", description: "Au lieu de coder un agent pour chaque besoin, tu construis une plateforme où les clients déploient des agents préconfigurés. Chaque agent ajouté multiplie ta valeur." },
      ]},
      { id: "1-11", type: "callout", variant: "tip", html: "<strong>Le déclic :</strong> Chaque fois qu'un client te demande quelque chose que tu as déjà fait pour un autre, c'est un signal. Ce besoin récurrent, c'est ton futur produit SaaS." },

      { id: "1-12", type: "heading", level: 3, text: "Les revenus récurrents changent tout" },
      { id: "1-13", type: "text", html: "<p>Le MRR (Monthly Recurring Revenue) transforme ton business de 3 manières :</p><ul><li><strong>Prévisibilité :</strong> tu sais combien tu gagnes le mois prochain avant même qu'il commence</li><li><strong>Compounding :</strong> chaque nouveau client s'ajoute aux précédents (pas de repartir à zéro chaque mois)</li><li><strong>Valorisation :</strong> les investisseurs et acheteurs valorisent un SaaS 5-10× son ARR, contre 1× pour du service</li></ul><p>Avec 30 clients à 300€/mois, tu as 9 000€/mois de MRR. Et tu peux dormir tranquille — le churn mensuel d'un bon SaaS B2B est de 2-5%, pas 100% comme en mode projet.</p>" },
      { id: "1-14", type: "callout", variant: "info", html: "<strong>Multi-tenant, concrètement :</strong> ça veut dire que ton application unique sert plusieurs clients (tenants) avec leurs propres données, configurations et utilisateurs. Une seule base de code, un seul déploiement, mais chaque client a l'impression d'avoir son propre produit." },
    ]),
    exercise: "<h3>Exercice : Identifie ton futur SaaS</h3><p>Reprends la liste de tous les projets que tu as réalisés (ou que tu pourrais réaliser) en freelance/agence. Pour chacun :</p><ol><li>Note le prix facturé et le temps passé</li><li>Identifie les fonctionnalités communes entre les projets</li><li>Estime combien de clients potentiels auraient le même besoin</li><li>Calcule : si tu transformais ce service en SaaS à 250€/mois, combien te faudrait-il de clients pour dépasser ton CA actuel ?</li></ol><p>Choisis le projet avec le plus de potentiel de récurrence et rédige un pitch d'une phrase : <em>\"[Mon SaaS] permet à [cible] de [bénéfice] pour [prix]/mois\"</em>.</p>",
    quiz: [
      { type: "mcq", question: "Quel est le problème principal du modèle sur-mesure ?", options: JSON.stringify(["Les clients paient trop cher", "Chaque euro gagné nécessite le même effort — tu échanges du temps contre de l'argent", "Il est impossible de trouver des clients", "La technologie est trop complexe"]), correctAnswer: "Chaque euro gagné nécessite le même effort — tu échanges du temps contre de l'argent", explanation: "Le sur-mesure est un piège : ton revenu est proportionnel à tes heures. Le modèle SaaS multi-tenant permet de découpler ton temps de tes revenus grâce au build once, sell many." },
      { type: "mcq", question: "Que signifie 'multi-tenant' ?", options: JSON.stringify(["Chaque client a sa propre application séparée", "Une seule application sert plusieurs clients avec leurs propres données isolées", "Les clients partagent les mêmes données entre eux", "Il faut déployer une instance par client"]), correctAnswer: "Une seule application sert plusieurs clients avec leurs propres données isolées", explanation: "Multi-tenant = une seule base de code et un seul déploiement servent N clients. Chaque client (tenant) a ses données isolées, ses configurations, mais partage l'infrastructure." },
      { type: "mcq", question: "Comment un SaaS B2B est-il généralement valorisé à la revente ?", options: JSON.stringify(["1× le CA annuel", "5-10× l'ARR (Annual Recurring Revenue)", "Le prix du développement initial", "50× le MRR"]), correctAnswer: "5-10× l'ARR (Annual Recurring Revenue)", explanation: "Un SaaS B2B avec des revenus récurrents se valorise typiquement entre 5 et 10 fois son ARR. Un business de services sur-mesure se valorise généralement autour de 1× le CA annuel." },
      { type: "true_false", question: "Le churn mensuel moyen d'un bon SaaS B2B est de 20-30%.", options: JSON.stringify(["Vrai", "Faux"]), correctAnswer: "Faux", explanation: "Le churn mensuel d'un bon SaaS B2B est plutôt de 2-5%. Un churn de 20-30% serait catastrophique et signifierait que tu perds la majorité de tes clients chaque trimestre." },
      { type: "mcq", question: "Quel est le meilleur signal qu'un service sur-mesure peut devenir un SaaS ?", options: JSON.stringify(["Tu n'aimes plus faire ce service", "Plusieurs clients te demandent la même chose avec des variations mineures", "Tu veux augmenter tes prix", "Ton concurrent a levé des fonds"]), correctAnswer: "Plusieurs clients te demandent la même chose avec des variations mineures", explanation: "Quand tu remarques un besoin récurrent chez plusieurs clients, c'est le signal parfait : tu peux standardiser la solution et la vendre comme produit. Les variations mineures deviennent des configurations par tenant." },
    ],
  },

  // ─── LEÇON 2 : Architecture multi-tenant avec Supabase ───
  {
    order: 2,
    module: 21,
    title: "Architecture multi-tenant avec Supabase",
    slug: "architecture-multitenant-supabase",
    duration: "25 min",
    description: "Comment structurer une base de données multi-tenant avec Supabase : RLS par organisation, isolation des données, schéma avec org_id, et les patterns d'architecture.",
    content: blocks([
      { id: "2-1", type: "heading", level: 2, text: "Les patterns d'architecture multi-tenant" },
      { id: "2-2", type: "text", html: "<p>Avant de coder quoi que ce soit, tu dois choisir ton pattern d'architecture. Il en existe 3, chacun avec ses compromis :</p>" },
      { id: "2-3", type: "comparison", headers: ["Pattern", "Principe", "Avantages", "Inconvénients"], rows: [
        { cells: ["Base partagée + colonne org_id", "Toutes les données dans les mêmes tables, filtrées par org_id", "Simple, économique, facile à maintenir", "Risque de fuite de données si mal configuré"] },
        { cells: ["Schéma par tenant", "Chaque client a son propre schéma PostgreSQL", "Bonne isolation, migrations plus complexes", "Plus lourd à gérer, limité en nombre de tenants"] },
        { cells: ["Base de données par tenant", "Chaque client a sa propre base de données", "Isolation maximale, conformité réglementaire", "Coûteux, complexe à maintenir et déployer"] },
      ]},
      { id: "2-4", type: "callout", variant: "tip", html: "<strong>Recommandation pour démarrer :</strong> Le pattern <strong>base partagée + org_id</strong> est le meilleur choix pour 95% des cas. C'est celui que Supabase supporte nativement avec RLS. Passe aux autres patterns seulement si tu as des contraintes réglementaires (santé, finance)." },
      { id: "2-5", type: "separator", style: "dots" },

      { id: "2-6", type: "heading", level: 2, text: "Le schéma multi-tenant : org_id partout" },
      { id: "2-7", type: "text", html: "<p>Le principe est simple : chaque table qui contient des données spécifiques à un client doit avoir une colonne <code>org_id</code>. Voici le schéma de base :</p><pre><code>-- Table des organisations (tenants)\nCREATE TABLE organizations (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  name TEXT NOT NULL,\n  slug TEXT UNIQUE NOT NULL,\n  settings JSONB DEFAULT '{}',\n  created_at TIMESTAMPTZ DEFAULT now()\n);\n\n-- Table de liaison users ↔ organizations\nCREATE TABLE org_members (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  org_id UUID REFERENCES organizations(id),\n  user_id UUID REFERENCES auth.users(id),\n  role TEXT DEFAULT 'member', -- 'owner', 'admin', 'member'\n  UNIQUE(org_id, user_id)\n);\n\n-- Exemple : table chatbots (multi-tenant)\nCREATE TABLE chatbots (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  org_id UUID REFERENCES organizations(id) NOT NULL,\n  name TEXT NOT NULL,\n  config JSONB DEFAULT '{}',\n  created_at TIMESTAMPTZ DEFAULT now()\n);\n\n-- Exemple : table conversations (multi-tenant)\nCREATE TABLE conversations (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  org_id UUID REFERENCES organizations(id) NOT NULL,\n  chatbot_id UUID REFERENCES chatbots(id) NOT NULL,\n  messages JSONB DEFAULT '[]',\n  created_at TIMESTAMPTZ DEFAULT now()\n);</code></pre><p>Remarque : <strong>org_id est NOT NULL partout</strong>. Aucune donnée ne peut exister sans être rattachée à une organisation. C'est ta garantie d'isolation.</p>" },
      { id: "2-8", type: "separator", style: "line" },

      { id: "2-9", type: "heading", level: 2, text: "Row Level Security (RLS) : le gardien des données" },
      { id: "2-10", type: "text", html: "<p>RLS, c'est le mécanisme de PostgreSQL qui filtre automatiquement les données au niveau de la base. Pas besoin de mettre des <code>WHERE org_id = ...</code> partout dans ton code — la base de données le fait pour toi.</p><pre><code>-- Activer RLS sur la table chatbots\nALTER TABLE chatbots ENABLE ROW LEVEL SECURITY;\n\n-- Fonction helper : récupérer les orgs de l'utilisateur courant\nCREATE OR REPLACE FUNCTION get_user_org_ids()\nRETURNS SETOF UUID AS $$\n  SELECT org_id FROM org_members\n  WHERE user_id = auth.uid()\n$$ LANGUAGE sql SECURITY DEFINER;\n\n-- Policy : un utilisateur ne voit que les chatbots de ses organisations\nCREATE POLICY \"Users can view their org chatbots\"\n  ON chatbots FOR SELECT\n  USING (org_id IN (SELECT get_user_org_ids()));\n\n-- Policy : un utilisateur ne peut insérer que dans ses organisations\nCREATE POLICY \"Users can insert in their orgs\"\n  ON chatbots FOR INSERT\n  WITH CHECK (org_id IN (SELECT get_user_org_ids()));\n\n-- Policy : seuls les admins/owners peuvent supprimer\nCREATE POLICY \"Admins can delete org chatbots\"\n  ON chatbots FOR DELETE\n  USING (\n    org_id IN (\n      SELECT org_id FROM org_members\n      WHERE user_id = auth.uid()\n      AND role IN ('owner', 'admin')\n    )\n  );</code></pre>" },
      { id: "2-11", type: "callout", variant: "warning", html: "<strong>Piège classique :</strong> Si tu oublies d'activer RLS sur une table, les données sont accessibles à TOUT LE MONDE. Supabase active RLS par défaut sur les nouvelles tables, mais vérifie toujours. Un seul oubli = une fuite de données catastrophique." },

      { id: "2-12", type: "heading", level: 3, text: "Côté application : le context organisationnel" },
      { id: "2-13", type: "text", html: "<p>Dans ton app Next.js, tu dois toujours savoir dans quelle organisation l'utilisateur travaille. Voici le pattern :</p><pre><code>// hooks/useOrganization.ts\nimport { createContext, useContext } from 'react';\n\ntype OrgContext = {\n  orgId: string;\n  orgSlug: string;\n  role: 'owner' | 'admin' | 'member';\n};\n\nconst OrganizationContext = createContext&lt;OrgContext | null&gt;(null);\n\nexport function useOrganization() {\n  const ctx = useContext(OrganizationContext);\n  if (!ctx) throw new Error('useOrganization must be used within OrgProvider');\n  return ctx;\n}\n\n// Utilisation dans un composant\nfunction ChatbotList() {\n  const { orgId } = useOrganization();\n  // Supabase filtre automatiquement via RLS,\n  // mais on passe quand même org_id pour la clarté\n  const { data } = await supabase\n    .from('chatbots')\n    .select('*')\n    .eq('org_id', orgId);\n}</code></pre>" },
      { id: "2-14", type: "checklist", title: "Checklist multi-tenant Supabase", items: [
        { id: "c1", text: "Toutes les tables métier ont une colonne org_id NOT NULL" },
        { id: "c2", text: "RLS est activé sur TOUTES les tables sans exception" },
        { id: "c3", text: "Les policies RLS utilisent get_user_org_ids() pour filtrer" },
        { id: "c4", text: "La table org_members gère les rôles (owner, admin, member)" },
        { id: "c5", text: "Un index est créé sur org_id pour chaque table (performance)" },
        { id: "c6", text: "Le context OrganizationProvider wrappe toute l'app" },
      ]},
    ]),
    exercise: "<h3>Exercice : Crée ton schéma multi-tenant</h3><p>Dans ton projet Supabase :</p><ol><li>Crée les tables <code>organizations</code>, <code>org_members</code>, et une table métier de ton choix (chatbots, documents, etc.)</li><li>Ajoute <code>org_id</code> à ta table métier avec une contrainte NOT NULL et une FOREIGN KEY</li><li>Crée la fonction <code>get_user_org_ids()</code></li><li>Écris les policies RLS pour SELECT, INSERT, UPDATE et DELETE</li><li>Teste en créant 2 organisations et en vérifiant qu'un membre de l'org A ne peut pas voir les données de l'org B</li></ol><p><strong>Bonus :</strong> Ajoute un index sur <code>org_id</code> et mesure la différence de performance avec <code>EXPLAIN ANALYZE</code>.</p>",
    quiz: [
      { type: "mcq", question: "Quel pattern multi-tenant est recommandé pour démarrer avec Supabase ?", options: JSON.stringify(["Base de données par tenant", "Schéma par tenant", "Base partagée avec colonne org_id + RLS", "Serveur dédié par client"]), correctAnswer: "Base partagée avec colonne org_id + RLS", explanation: "Le pattern base partagée + org_id est le plus simple, le plus économique, et nativement supporté par Supabase via RLS. Il couvre 95% des cas d'usage." },
      { type: "mcq", question: "Que fait RLS (Row Level Security) ?", options: JSON.stringify(["Il chiffre les données au repos", "Il filtre automatiquement les lignes au niveau de la base de données selon des policies", "Il empêche les injections SQL", "Il crée des backups automatiques"]), correctAnswer: "Il filtre automatiquement les lignes au niveau de la base de données selon des policies", explanation: "RLS applique des filtres automatiques sur chaque requête. Si ta policy dit 'un user ne voit que les données de son org', PostgreSQL ajoute ce filtre à chaque SELECT, même si ton code oublie le WHERE." },
      { type: "true_false", question: "Si tu oublies d'activer RLS sur une table Supabase, les données de cette table sont accessibles à tous les utilisateurs authentifiés.", options: JSON.stringify(["Vrai", "Faux"]), correctAnswer: "Vrai", explanation: "Sans RLS, il n'y a aucun filtrage au niveau base de données. N'importe quel utilisateur authentifié peut requêter toutes les lignes de la table. C'est une faille de sécurité critique dans un contexte multi-tenant." },
      { type: "mcq", question: "Pourquoi la colonne org_id doit être NOT NULL ?", options: JSON.stringify(["Pour améliorer les performances", "Pour garantir qu'aucune donnée n'existe sans être rattachée à une organisation", "Pour que les index fonctionnent", "C'est une convention mais pas obligatoire"]), correctAnswer: "Pour garantir qu'aucune donnée n'existe sans être rattachée à une organisation", explanation: "Si une donnée peut exister sans org_id, elle échappe à l'isolation multi-tenant. NOT NULL garantit que chaque ligne appartient à une organisation identifiée." },
      { type: "mcq", question: "Quel est l'intérêt de créer un index sur la colonne org_id ?", options: JSON.stringify(["C'est obligatoire pour que RLS fonctionne", "Ça améliore drastiquement les performances de filtrage quand la base grandit", "Ça empêche les doublons", "Ça chiffre la colonne"]), correctAnswer: "Ça améliore drastiquement les performances de filtrage quand la base grandit", explanation: "Chaque requête filtre par org_id (via RLS). Sans index, PostgreSQL doit scanner toute la table. Avec un index, le filtrage est quasi instantané même avec des millions de lignes." },
    ],
  },

  // ─── LEÇON 3 : White-label : personnaliser sans dupliquer ───
  {
    order: 3,
    module: 21,
    title: "White-label : personnaliser sans dupliquer",
    slug: "white-label-personnaliser-sans-dupliquer",
    duration: "25 min",
    description: "Comment offrir à chaque client un produit à son image — domaine custom, logo, couleurs — sans dupliquer le code. DNS, certificats SSL, et configuration par tenant.",
    content: blocks([
      { id: "3-1", type: "heading", level: 2, text: "White-label : ton produit, leur marque" },
      { id: "3-2", type: "text", html: "<p>Le white-label, c'est la capacité de ton SaaS à <strong>s'habiller aux couleurs de chaque client</strong>. Ton client final ne sait même pas que c'est ton produit — il voit le logo de son prestataire, ses couleurs, son domaine.</p><p>C'est un <strong>argument de vente massif</strong> pour les agences et revendeurs : ils veulent un produit qu'ils peuvent présenter comme le leur. Et toi, tu touches un abonnement récurrent sans effort supplémentaire.</p>" },
      { id: "3-3", type: "callout", variant: "info", html: "<strong>Exemples concrets :</strong> Intercom en white-label pour les agences digitales. Typeform personnalisé aux couleurs du client. Un chatbot IA avec le logo et le domaine de chaque entreprise cliente." },
      { id: "3-4", type: "separator", style: "dots" },

      { id: "3-5", type: "heading", level: 2, text: "Variables de configuration par tenant" },
      { id: "3-6", type: "text", html: "<p>Chaque organisation a ses propres paramètres visuels et fonctionnels. Tout est stocké dans une colonne JSONB <code>settings</code> :</p><pre><code>-- Structure de la config par tenant\n{\n  \"branding\": {\n    \"logo_url\": \"https://cdn.example.com/client-a/logo.png\",\n    \"favicon_url\": \"https://cdn.example.com/client-a/favicon.ico\",\n    \"primary_color\": \"#FF6B35\",\n    \"secondary_color\": \"#004E89\",\n    \"font_family\": \"Inter\",\n    \"company_name\": \"AgenceX\"\n  },\n  \"custom_domain\": \"chat.agencex.com\",\n  \"features\": {\n    \"analytics\": true,\n    \"export_csv\": true,\n    \"api_access\": false\n  },\n  \"email\": {\n    \"from_name\": \"AgenceX Support\",\n    \"reply_to\": \"support@agencex.com\"\n  }\n}</code></pre><p>Côté React, tu charges cette config et tu l'injectes dans un <strong>ThemeProvider</strong> :</p><pre><code>// providers/TenantThemeProvider.tsx\nfunction TenantThemeProvider({ children, orgSettings }) {\n  const theme = {\n    colors: {\n      primary: orgSettings.branding.primary_color,\n      secondary: orgSettings.branding.secondary_color,\n    },\n    font: orgSettings.branding.font_family,\n  };\n\n  return (\n    &lt;ThemeContext.Provider value={theme}&gt;\n      &lt;style&gt;{`:root {\n        --color-primary: ${theme.colors.primary};\n        --color-secondary: ${theme.colors.secondary};\n        --font-main: ${theme.font}, sans-serif;\n      }`}&lt;/style&gt;\n      {children}\n    &lt;/ThemeContext.Provider&gt;\n  );\n}</code></pre>" },
      { id: "3-7", type: "separator", style: "line" },

      { id: "3-8", type: "heading", level: 2, text: "Domaines custom : DNS et certificats SSL" },
      { id: "3-9", type: "text", html: "<p>Chaque client veut accéder à ton produit via <strong>son propre domaine</strong> : <code>chat.client-a.com</code> au lieu de <code>app.tonproduit.com/client-a</code>. Voici comment ça fonctionne :</p>" },
      { id: "3-10", type: "steps", steps: [
        { title: "1. Le client configure un CNAME DNS", description: "Le client ajoute un enregistrement CNAME sur son domaine : chat.client-a.com → proxy.tonproduit.com. C'est une opération de 2 minutes chez son registrar (OVH, Cloudflare, etc.)." },
        { title: "2. Ton serveur détecte le domaine entrant", description: "À chaque requête, tu lis le header Host pour identifier quel tenant est concerné. Tu fais un lookup dans ta table organizations WHERE custom_domain = request.host." },
        { title: "3. Certificat SSL automatique", description: "Tu utilises Caddy, Traefik, ou Vercel pour générer automatiquement un certificat Let's Encrypt pour chaque domaine custom. Zéro intervention manuelle." },
        { title: "4. Rendu personnalisé", description: "Une fois le tenant identifié, tu charges ses settings (logo, couleurs, features) et tu rendres l'app avec sa configuration. Même code, apparence différente." },
      ]},
      { id: "3-11", type: "callout", variant: "tip", html: "<strong>Raccourci Vercel :</strong> Si tu déploies sur Vercel, tu peux utiliser l'API Domains pour ajouter des domaines custom programmatiquement. Vercel gère les certificats SSL automatiquement. C'est de loin la solution la plus simple." },

      { id: "3-12", type: "heading", level: 3, text: "Résolution du tenant dans Next.js" },
      { id: "3-13", type: "text", html: "<p>Voici le middleware Next.js qui identifie le tenant à chaque requête :</p><pre><code>// middleware.ts\nimport { NextRequest, NextResponse } from 'next/server';\nimport { createClient } from '@supabase/supabase-js';\n\nexport async function middleware(req: NextRequest) {\n  const hostname = req.headers.get('host') || '';\n  \n  // Ignorer les domaines internes\n  if (hostname.includes('tonproduit.com')) {\n    return NextResponse.next();\n  }\n\n  // Lookup du tenant par domaine custom\n  const supabase = createClient(/* ... */);\n  const { data: org } = await supabase\n    .from('organizations')\n    .select('id, slug, settings')\n    .eq('custom_domain', hostname)\n    .single();\n\n  if (!org) {\n    return NextResponse.redirect('https://tonproduit.com');\n  }\n\n  // Injecter l'org_id dans les headers pour le reste de l'app\n  const response = NextResponse.next();\n  response.headers.set('x-org-id', org.id);\n  response.headers.set('x-org-slug', org.slug);\n  return response;\n}</code></pre>" },
      { id: "3-14", type: "callout", variant: "warning", html: "<strong>Performance :</strong> Ne fais pas un appel Supabase à chaque requête middleware ! Utilise un cache en mémoire (Map ou Redis) avec un TTL de 5 minutes pour stocker le mapping domaine → org_id. Sinon, tu ajoutes 50-100ms de latence à chaque page." },
      { id: "3-15", type: "checklist", title: "Checklist white-label", items: [
        { id: "c1", text: "Config branding (logo, couleurs, fonts) stockée en JSONB par org" },
        { id: "c2", text: "ThemeProvider qui injecte les CSS custom variables" },
        { id: "c3", text: "Middleware de résolution du tenant par domaine" },
        { id: "c4", text: "Cache du mapping domaine → org_id (5 min TTL)" },
        { id: "c5", text: "Certificats SSL automatiques (Let's Encrypt / Vercel)" },
        { id: "c6", text: "Interface admin pour que le client configure son branding" },
      ]},
    ]),
    exercise: "<h3>Exercice : Implémente le white-label</h3><ol><li>Ajoute une colonne <code>settings JSONB</code> à ta table <code>organizations</code> avec la structure branding (logo, couleurs, nom)</li><li>Crée un composant <code>TenantThemeProvider</code> qui lit les settings et injecte les CSS variables</li><li>Crée le middleware Next.js qui résout le tenant à partir du header Host</li><li>Crée une page admin <code>/settings/branding</code> où le client peut uploader son logo et choisir ses couleurs</li><li>Teste avec 2 organisations différentes : vérifie que l'apparence change selon le domaine</li></ol><p><strong>Astuce :</strong> Pour tester en local, ajoute des entrées dans ton fichier <code>/etc/hosts</code> : <code>127.0.0.1 client-a.local client-b.local</code></p>",
    quiz: [
      { type: "mcq", question: "Qu'est-ce que le white-label dans le contexte SaaS ?", options: JSON.stringify(["Vendre un produit sans marque", "Permettre à chaque client d'afficher son propre branding sur ton produit", "Créer un produit open-source", "Dupliquer le code pour chaque client"]), correctAnswer: "Permettre à chaque client d'afficher son propre branding sur ton produit", explanation: "Le white-label permet à chaque client de présenter ton produit comme le sien : son logo, ses couleurs, son domaine. Tu gardes une seule base de code, mais chaque client a l'impression d'avoir son propre produit." },
      { type: "mcq", question: "Comment un client configure-t-il un domaine custom ?", options: JSON.stringify(["Il achète une licence spéciale", "Il ajoute un enregistrement CNAME DNS pointant vers ton serveur", "Il doit t'envoyer ses credentials DNS", "Il installe un plugin sur son site"]), correctAnswer: "Il ajoute un enregistrement CNAME DNS pointant vers ton serveur", explanation: "Le client ajoute un CNAME record (ex: chat.client.com → proxy.tonproduit.com) chez son registrar. Ton serveur détecte le domaine entrant et charge la configuration du bon tenant." },
      { type: "true_false", question: "Il faut générer manuellement un certificat SSL pour chaque domaine custom.", options: JSON.stringify(["Vrai", "Faux"]), correctAnswer: "Faux", explanation: "Les certificats SSL sont générés automatiquement via Let's Encrypt. Des outils comme Caddy, Traefik ou Vercel gèrent ça sans intervention manuelle." },
      { type: "mcq", question: "Pourquoi faut-il cacher le mapping domaine → org_id ?", options: JSON.stringify(["Pour des raisons de sécurité", "Pour éviter un appel base de données à chaque requête HTTP (50-100ms de latence)", "Pour pouvoir fonctionner hors-ligne", "Ce n'est pas nécessaire"]), correctAnswer: "Pour éviter un appel base de données à chaque requête HTTP (50-100ms de latence)", explanation: "Le middleware s'exécute à CHAQUE requête. Sans cache, chaque page load fait un appel Supabase juste pour identifier le tenant. Un cache avec un TTL de 5 minutes élimine cette latence." },
      { type: "mcq", question: "Où stocker les paramètres de branding (logo, couleurs) de chaque tenant ?", options: JSON.stringify(["Dans des fichiers CSS séparés par client", "Dans des variables d'environnement", "Dans une colonne JSONB de la table organizations", "Dans le code source de l'application"]), correctAnswer: "Dans une colonne JSONB de la table organizations", explanation: "JSONB est idéal : c'est flexible (pas de migration pour ajouter un champ), requêtable, et stocké directement avec les données du tenant. Les CSS variables sont générées dynamiquement à partir de ces données." },
    ],
  },

  // ─── LEÇON 4 : Billing multi-tenant : Stripe Connect ───
  {
    order: 4,
    module: 21,
    title: "Billing multi-tenant : Stripe Connect",
    slug: "billing-multitenant-stripe-connect",
    duration: "25 min",
    description: "Comment gérer la facturation multi-tenant avec Stripe Connect : facturer les clients de tes clients, gérer les abonnements par organisation, et prendre des commissions.",
    content: blocks([
      { id: "4-1", type: "heading", level: 2, text: "Le problème de la facturation multi-tenant" },
      { id: "4-2", type: "text", html: "<p>Dans un SaaS classique, tu factures directement tes clients. Mais dans un SaaS multi-tenant destiné aux agences, il y a <strong>3 niveaux de facturation</strong> :</p><ul><li><strong>Toi</strong> → tu factures l'agence (ton client direct)</li><li><strong>L'agence</strong> → elle facture ses propres clients finaux</li><li><strong>Optionnel :</strong> Tu factures directement les clients finaux et tu reverses une commission à l'agence</li></ul><p>C'est exactement ce que <strong>Stripe Connect</strong> est conçu pour gérer. C'est la même infrastructure que Shopify, Uber, ou Deliveroo utilisent.</p>" },
      { id: "4-3", type: "callout", variant: "info", html: "<strong>Stripe Connect en une phrase :</strong> C'est une API qui permet à ta plateforme de gérer les paiements entre plusieurs parties — toi, tes clients (agences), et les clients de tes clients." },
      { id: "4-4", type: "separator", style: "dots" },

      { id: "4-5", type: "heading", level: 2, text: "Les 3 modèles Stripe Connect" },
      { id: "4-6", type: "comparison", headers: ["Modèle", "Comment ça marche", "Cas d'usage"], rows: [
        { cells: ["Standard", "Chaque agence a son propre compte Stripe. Tu rediriges vers leur dashboard.", "L'agence veut un contrôle total sur sa facturation"] },
        { cells: ["Express", "Tu crées un compte Stripe simplifié pour chaque agence. Onboarding rapide.", "L'agence veut un setup simple, tu gères la complexité"] },
        { cells: ["Custom", "Tu gères tout : le compte, l'UI de facturation, les payouts. Invisible pour l'agence.", "Tu veux une expérience 100% white-label, même la facturation"] },
      ]},
      { id: "4-7", type: "callout", variant: "tip", html: "<strong>Recommandation :</strong> Commence avec <strong>Express</strong>. L'onboarding est simple (un lien), Stripe gère le KYC et la conformité, et tu peux prendre des commissions automatiquement. Passe à Custom seulement si tu veux masquer complètement Stripe." },
      { id: "4-8", type: "separator", style: "line" },

      { id: "4-9", type: "heading", level: 2, text: "Implémenter Stripe Connect Express" },
      { id: "4-10", type: "text", html: "<p>Voici le flow complet, étape par étape :</p><pre><code>// 1. Créer un compte Connect pour chaque organisation\nimport Stripe from 'stripe';\nconst stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);\n\nasync function createConnectedAccount(org: Organization) {\n  const account = await stripe.accounts.create({\n    type: 'express',\n    country: 'FR',\n    email: org.ownerEmail,\n    capabilities: {\n      card_payments: { requested: true },\n      transfers: { requested: true },\n    },\n    metadata: { org_id: org.id },\n  });\n\n  // Sauvegarder l'ID du compte Stripe dans la table orgs\n  await supabase\n    .from('organizations')\n    .update({ stripe_account_id: account.id })\n    .eq('id', org.id);\n\n  return account;\n}\n\n// 2. Générer le lien d'onboarding\nasync function getOnboardingLink(stripeAccountId: string) {\n  const link = await stripe.accountLinks.create({\n    account: stripeAccountId,\n    refresh_url: 'https://app.tonproduit.com/settings/billing',\n    return_url: 'https://app.tonproduit.com/settings/billing?setup=complete',\n    type: 'account_onboarding',\n  });\n  return link.url;\n}\n\n// 3. Créer un abonnement pour le client final\nasync function createSubscription(\n  orgStripeAccountId: string,\n  customerEmail: string,\n  priceId: string,\n  applicationFeePercent: number\n) {\n  const customer = await stripe.customers.create(\n    { email: customerEmail },\n    { stripeAccount: orgStripeAccountId }\n  );\n\n  const subscription = await stripe.subscriptions.create(\n    {\n      customer: customer.id,\n      items: [{ price: priceId }],\n      application_fee_percent: applicationFeePercent, // Ta commission !\n    },\n    { stripeAccount: orgStripeAccountId }\n  );\n\n  return subscription;\n}</code></pre>" },
      { id: "4-11", type: "callout", variant: "warning", html: "<strong>Attention KYC :</strong> Stripe exige une vérification d'identité (KYC) pour chaque compte Connect. L'agence devra fournir ses documents. Si elle ne termine pas l'onboarding, les payouts sont bloqués. Prévois un flow de relance." },

      { id: "4-12", type: "heading", level: 3, text: "Le modèle de commission (application fees)" },
      { id: "4-13", type: "text", html: "<p>Le nerf de la guerre : comment gagner de l'argent sur chaque transaction. Stripe Connect propose 2 mécanismes :</p><ul><li><strong>application_fee_percent :</strong> tu prends un pourcentage sur chaque paiement (ex: 10%). C'est le modèle marketplace classique.</li><li><strong>application_fee_amount :</strong> tu prends un montant fixe par transaction (ex: 2€). Utile pour les petits montants.</li></ul><p>Exemple concret : ton client agence vend un chatbot à 200€/mois à son client final. Avec 10% de commission, tu touches <strong>20€/mois</strong> automatiquement, sans rien faire. Avec 50 agences qui ont chacune 10 clients… 50 × 10 × 20€ = <strong>10 000€/mois de commissions passives</strong>.</p>" },

      { id: "4-14", type: "diagram", variant: "flow", nodes: [
        { id: "n1", label: "Client final", description: "Paie 200€/mois pour le chatbot" },
        { id: "n2", label: "Stripe Connect", description: "Collecte le paiement, prélève les frais Stripe (~3%)" },
        { id: "n3", label: "Ta plateforme", description: "Reçoit 10% de commission = 20€" },
        { id: "n4", label: "Agence (tenant)", description: "Reçoit le reste = ~174€" },
      ]},
    ]),
    exercise: "<h3>Exercice : Configure Stripe Connect</h3><ol><li>Crée un compte Stripe et active Connect dans le dashboard (mode test)</li><li>Implémente l'endpoint API <code>/api/stripe/connect/create-account</code> qui crée un compte Express pour une organisation</li><li>Implémente l'endpoint <code>/api/stripe/connect/onboarding-link</code> qui génère le lien d'onboarding</li><li>Crée une page <code>/settings/billing</code> dans ton app qui affiche le statut du compte Connect et le bouton d'onboarding</li><li>Implémente un webhook qui écoute <code>account.updated</code> pour mettre à jour le statut du compte dans ta DB</li></ol><p><strong>Bonus :</strong> Crée un dashboard qui affiche le MRR total, les commissions gagnées, et le nombre de clients par agence.</p>",
    quiz: [
      { type: "mcq", question: "Qu'est-ce que Stripe Connect ?", options: JSON.stringify(["Un plugin WordPress pour les paiements", "Une API qui permet à une plateforme de gérer les paiements entre plusieurs parties", "Un service de facturation automatique", "Un concurrent de PayPal"]), correctAnswer: "Une API qui permet à une plateforme de gérer les paiements entre plusieurs parties", explanation: "Stripe Connect est conçu pour les plateformes et marketplaces. Il gère les paiements entre toi (la plateforme), tes clients (les agences), et les clients finaux — avec commissions automatiques." },
      { type: "mcq", question: "Quel modèle Stripe Connect est recommandé pour démarrer ?", options: JSON.stringify(["Standard — contrôle total pour l'agence", "Express — onboarding simple, Stripe gère la conformité", "Custom — tu gères tout toi-même", "Direct — sans intermédiaire"]), correctAnswer: "Express — onboarding simple, Stripe gère la conformité", explanation: "Express offre le meilleur compromis : onboarding rapide pour l'agence, conformité KYC gérée par Stripe, et tu peux prendre des commissions automatiquement. Custom est trop complexe pour démarrer." },
      { type: "mcq", question: "Qu'est-ce que application_fee_percent dans Stripe Connect ?", options: JSON.stringify(["Les frais bancaires de Stripe", "Le pourcentage de commission que ta plateforme prélève sur chaque paiement", "La TVA applicable", "Le taux de conversion des devises"]), correctAnswer: "Le pourcentage de commission que ta plateforme prélève sur chaque paiement", explanation: "application_fee_percent te permet de prendre automatiquement un pourcentage sur chaque paiement qui passe par ta plateforme. C'est le modèle de revenus classique des marketplaces." },
      { type: "true_false", question: "Avec Stripe Connect Express, l'agence n'a pas besoin de passer par une vérification d'identité (KYC).", options: JSON.stringify(["Vrai", "Faux"]), correctAnswer: "Faux", explanation: "Stripe exige un KYC pour chaque compte Connect, même en mode Express. L'agence doit fournir ses documents d'identité et informations bancaires. Si le KYC n'est pas complété, les payouts sont bloqués." },
      { type: "mcq", question: "Avec 50 agences ayant chacune 10 clients à 200€/mois et une commission de 10%, combien gagnes-tu par mois ?", options: JSON.stringify(["1 000€/mois", "5 000€/mois", "10 000€/mois", "20 000€/mois"]), correctAnswer: "10 000€/mois", explanation: "50 agences × 10 clients × 200€ × 10% = 10 000€/mois de commissions. Et c'est du revenu passif — les paiements sont automatiques, tu n'as rien à faire." },
    ],
  },

  // ─── LEÇON 5 : Atelier — transformer un projet one-shot en SaaS multi-tenant ───
  {
    order: 5,
    module: 21,
    title: "Atelier : transformer un projet one-shot en SaaS multi-tenant",
    slug: "atelier-projet-oneshot-saas-multitenant",
    duration: "30 min",
    description: "Prendre le chatbot construit dans un module précédent et le transformer en SaaS multi-tenant. Chaque client a son chatbot personnalisé, son domaine, sa facturation.",
    content: blocks([
      { id: "5-1", type: "heading", level: 2, text: "L'objectif : du chatbot one-shot au SaaS" },
      { id: "5-2", type: "text", html: "<p>Tu as construit un chatbot IA dans les modules précédents. Aujourd'hui, on va le transformer en <strong>plateforme SaaS multi-tenant</strong>. Le résultat final :</p><ul><li>Chaque agence crée un compte et configure son chatbot</li><li>Chaque agence a son propre domaine et branding</li><li>Les conversations sont isolées par organisation</li><li>La facturation est automatique via Stripe Connect</li><li>Toi, tu touches une commission sur chaque abonnement</li></ul><p>On va assembler tout ce qu'on a vu dans les leçons précédentes en un projet concret.</p>" },
      { id: "5-3", type: "callout", variant: "info", html: "<strong>Ce qu'on va construire :</strong> Une plateforme de type 'Chatbot-as-a-Service'. Chaque client s'inscrit, configure son chatbot (ton, instructions, couleurs), le déploie sur son site, et paie un abonnement mensuel. Toi, tu gères la plateforme et tu encaisses." },
      { id: "5-4", type: "separator", style: "dots" },

      { id: "5-5", type: "heading", level: 2, text: "Étape 1 : Le schéma de base de données" },
      { id: "5-6", type: "text", html: "<p>On part du schéma multi-tenant vu en leçon 2 et on l'enrichit pour notre chatbot SaaS :</p><pre><code>-- Tables existantes\n-- organizations (id, name, slug, settings, stripe_account_id, ...)\n-- org_members (id, org_id, user_id, role)\n\n-- Nouvelle table : chatbot configs par org\nCREATE TABLE chatbot_configs (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  org_id UUID REFERENCES organizations(id) NOT NULL,\n  name TEXT NOT NULL DEFAULT 'Mon Chatbot',\n  system_prompt TEXT NOT NULL DEFAULT 'Tu es un assistant utile.',\n  model TEXT NOT NULL DEFAULT 'gpt-4o-mini',\n  temperature FLOAT DEFAULT 0.7,\n  welcome_message TEXT DEFAULT 'Bonjour ! Comment puis-je vous aider ?',\n  widget_position TEXT DEFAULT 'bottom-right',\n  widget_color TEXT DEFAULT '#6366f1',\n  allowed_domains TEXT[] DEFAULT '{}',\n  created_at TIMESTAMPTZ DEFAULT now()\n);\n\n-- Conversations multi-tenant\nCREATE TABLE chat_conversations (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  org_id UUID REFERENCES organizations(id) NOT NULL,\n  chatbot_id UUID REFERENCES chatbot_configs(id) NOT NULL,\n  visitor_id TEXT NOT NULL,\n  status TEXT DEFAULT 'active',\n  created_at TIMESTAMPTZ DEFAULT now()\n);\n\n-- Messages\nCREATE TABLE chat_messages (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  org_id UUID REFERENCES organizations(id) NOT NULL,\n  conversation_id UUID REFERENCES chat_conversations(id) NOT NULL,\n  role TEXT NOT NULL, -- 'user' | 'assistant'\n  content TEXT NOT NULL,\n  created_at TIMESTAMPTZ DEFAULT now()\n);\n\n-- RLS sur toutes les tables\nALTER TABLE chatbot_configs ENABLE ROW LEVEL SECURITY;\nALTER TABLE chat_conversations ENABLE ROW LEVEL SECURITY;\nALTER TABLE chat_messages ENABLE ROW LEVEL SECURITY;</code></pre>" },
      { id: "5-7", type: "separator", style: "line" },

      { id: "5-8", type: "heading", level: 2, text: "Étape 2 : L'architecture de l'application" },
      { id: "5-9", type: "diagram", variant: "flow", nodes: [
        { id: "n1", label: "Dashboard Agence", description: "/dashboard — gestion chatbot, analytics, billing" },
        { id: "n2", label: "API Multi-tenant", description: "/api/chat — identifie le tenant via API key" },
        { id: "n3", label: "Widget Embeddable", description: "<script> à coller sur le site du client final" },
        { id: "n4", label: "Supabase + RLS", description: "Données isolées par org_id automatiquement" },
        { id: "n5", label: "Stripe Connect", description: "Facturation par org, commissions automatiques" },
      ]},
      { id: "5-10", type: "text", html: "<p>L'app a 3 faces :</p><ol><li><strong>Le dashboard</strong> (pour l'agence) : configurer le chatbot, voir les conversations, gérer la facturation</li><li><strong>L'API</strong> (pour le widget) : recevoir les messages, répondre avec l'IA, isoler par tenant</li><li><strong>Le widget</strong> (pour le client final) : un bout de JS à coller sur n'importe quel site</li></ol>" },

      { id: "5-11", type: "heading", level: 2, text: "Étape 3 : Le widget embeddable" },
      { id: "5-12", type: "text", html: "<p>Le widget est la pièce maîtresse. C'est un script JavaScript que le client final colle sur son site :</p><pre><code>&lt;!-- Widget chatbot — à coller avant &lt;/body&gt; --&gt;\n&lt;script\n  src=\"https://app.tonproduit.com/widget.js\"\n  data-chatbot-id=\"abc123\"\n  data-api-key=\"pk_live_xxx\"\n&gt;&lt;/script&gt;</code></pre><p>Le script <code>widget.js</code> :</p><pre><code>// widget.js (simplifié)\n(function() {\n  const config = document.currentScript.dataset;\n  \n  // Créer l'iframe du chatbot\n  const iframe = document.createElement('iframe');\n  iframe.src = `https://app.tonproduit.com/embed/${config.chatbotId}`;\n  iframe.style.cssText = `\n    position: fixed; bottom: 20px; right: 20px;\n    width: 400px; height: 600px; border: none;\n    border-radius: 16px; box-shadow: 0 8px 32px rgba(0,0,0,0.15);\n    z-index: 999999; display: none;\n  `;\n  document.body.appendChild(iframe);\n\n  // Bouton d'ouverture\n  const btn = document.createElement('button');\n  btn.innerHTML = '💬';\n  btn.style.cssText = `\n    position: fixed; bottom: 20px; right: 20px;\n    width: 60px; height: 60px; border-radius: 50%;\n    border: none; cursor: pointer; z-index: 999999;\n    font-size: 24px;\n  `;\n  btn.onclick = () => {\n    const visible = iframe.style.display !== 'none';\n    iframe.style.display = visible ? 'none' : 'block';\n    btn.style.display = visible ? 'block' : 'none';\n  };\n  document.body.appendChild(btn);\n\n  // Charger les couleurs du tenant\n  fetch(`https://app.tonproduit.com/api/widget-config/${config.chatbotId}`)\n    .then(r => r.json())\n    .then(c => { btn.style.backgroundColor = c.widget_color; });\n})();</code></pre>" },
      { id: "5-13", type: "separator", style: "line" },

      { id: "5-14", type: "heading", level: 2, text: "Étape 4 : Le plan de lancement" },
      { id: "5-15", type: "steps", steps: [
        { title: "Semaine 1 : MVP multi-tenant", description: "Schéma DB avec org_id + RLS, inscription/création d'org, dashboard basique avec config chatbot. Objectif : un chatbot fonctionnel par organisation." },
        { title: "Semaine 2 : Widget + API", description: "Widget embeddable, API de chat avec isolation par tenant, page embed dans une iframe. Objectif : le client peut coller le widget sur son site." },
        { title: "Semaine 3 : White-label + billing", description: "Config branding par org, domaine custom (optionnel), intégration Stripe Connect avec abonnements. Objectif : le client paie, tu encaisses." },
        { title: "Semaine 4 : Analytics + polish", description: "Dashboard analytics (conversations, messages, satisfaction), onboarding guidé pour les nouvelles agences, documentation API. Objectif : prêt pour les premiers beta-testeurs." },
      ]},
      { id: "5-16", type: "callout", variant: "success", html: "<strong>Résultat final :</strong> En 4 semaines, tu as un SaaS fonctionnel que tu peux vendre à des agences. Chaque agence paie 200-500€/mois, tu prends 10-15% de commission sur les clients finaux. Avec 20 agences actives, tu as un business à 5-10K€/mois de MRR." },
    ]),
    exercise: "<h3>Atelier complet : Construis ton SaaS multi-tenant</h3><p>C'est le moment de tout assembler. Voici ton plan d'action :</p><ol><li><strong>Base de données :</strong> Crée les tables <code>organizations</code>, <code>org_members</code>, <code>chatbot_configs</code>, <code>chat_conversations</code>, <code>chat_messages</code> avec RLS activé et policies configurées</li><li><strong>Auth + Org :</strong> Implémente le flow d'inscription : créer un user → créer une org → associer le user comme owner</li><li><strong>Dashboard :</strong> Crée la page de configuration du chatbot (system prompt, modèle, couleur du widget, message de bienvenue)</li><li><strong>API Chat :</strong> Crée l'endpoint <code>/api/chat</code> qui identifie le tenant via API key, charge la config du chatbot, et répond avec l'IA</li><li><strong>Widget :</strong> Crée le fichier <code>widget.js</code> et la page <code>/embed/[chatbotId]</code></li></ol><p><strong>Critères de validation :</strong></p><ul><li>2 organisations différentes avec des chatbots distincts</li><li>Les conversations de l'org A ne sont pas visibles par l'org B</li><li>Le widget fonctionne sur un site externe</li><li>Chaque chatbot a ses propres couleurs et comportement</li></ul>",
    quiz: [
      { type: "mcq", question: "Quels sont les 3 composants principaux d'un SaaS chatbot multi-tenant ?", options: JSON.stringify(["Frontend, backend, base de données", "Dashboard agence, API multi-tenant, widget embeddable", "Design, développement, marketing", "Serveur, CDN, DNS"]), correctAnswer: "Dashboard agence, API multi-tenant, widget embeddable", explanation: "Le dashboard permet à l'agence de configurer son chatbot, l'API gère les conversations avec isolation par tenant, et le widget est le script que le client final colle sur son site." },
      { type: "mcq", question: "Comment le widget identifie-t-il quel chatbot utiliser ?", options: JSON.stringify(["Via un cookie", "Via les attributs data-chatbot-id et data-api-key du script", "Via l'URL de la page", "Via le navigateur de l'utilisateur"]), correctAnswer: "Via les attributs data-chatbot-id et data-api-key du script", explanation: "Le script utilise les attributs data-* pour identifier le chatbot et s'authentifier auprès de l'API. Chaque client a son propre chatbot-id et API key." },
      { type: "true_false", question: "Pour un SaaS multi-tenant, il faut déployer une instance séparée de l'application pour chaque client.", options: JSON.stringify(["Vrai", "Faux"]), correctAnswer: "Faux", explanation: "C'est tout l'intérêt du multi-tenant : une seule instance de l'application sert tous les clients. L'isolation se fait au niveau des données (RLS + org_id), pas au niveau de l'infrastructure." },
      { type: "mcq", question: "En combien de temps peut-on avoir un MVP de SaaS multi-tenant fonctionnel ?", options: JSON.stringify(["1 jour", "4 semaines avec un plan structuré", "6 mois minimum", "1 an"]), correctAnswer: "4 semaines avec un plan structuré", explanation: "Avec les outils modernes (Supabase, Next.js, Stripe Connect), un MVP fonctionnel est réalisable en 4 semaines : semaine 1 pour le multi-tenant, semaine 2 pour le widget, semaine 3 pour le billing, semaine 4 pour le polish." },
      { type: "mcq", question: "Pourquoi utiliser une iframe pour le widget chatbot plutôt que d'injecter du HTML directement ?", options: JSON.stringify(["C'est plus rapide", "L'iframe isole le CSS et JS du widget par rapport au site hôte, évitant les conflits", "Les iframes sont plus jolies", "C'est obligatoire pour le HTTPS"]), correctAnswer: "L'iframe isole le CSS et JS du widget par rapport au site hôte, évitant les conflits", explanation: "L'iframe crée un contexte isolé : le CSS du site hôte n'affecte pas le widget, et le JS du widget n'interfère pas avec le site. Sans iframe, les styles et scripts pourraient entrer en conflit." },
    ],
  },
];
